app = "novi-ai-projects"
primary_region = "cdg"

[build]
  dockerfile = "Dockerfile"

[[services]]
  internal_port = 80

  [[services.ports]]
    port = 80
    handlers = ["http"]

  [[services.ports]]
    port = 443
    handlers = ["tls", "http"]
